on:
  push:
  workflow_dispatch:

jobs:
  helloworld:
    runs-on: ubuntu-latest        
    steps:
      - name: Get a clone of the current repository
        uses: actions/checkout@v5   #plugings
          #with : <---and aditional parameters

      - name : list all the files
        run : ls    #To run a shell command
      - name: run the tests
        run : ./gradlew test   # --scan <-- for get deb

